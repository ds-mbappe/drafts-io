{
  "name": "drafts-io",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:front": "pnpm --filter frontend dev",
    "dev:back": "pnpm --filter backend build && pnpm --filter backend start:dev",
    "setup": "pnpm install && pnpm build:back"
  },
  "dependencies": {
  },
  "pnpm": {
    "overrides": {
      "@tiptap/core": "3.23.6",
      "@tiptap/pm": "3.23.6",
      "prosemirror-model": "1.25.5",
      "jws": ">=3.2.3",
      "protobufjs": ">=7.5.5"
    }
  }
}